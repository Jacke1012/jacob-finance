name: Build & Push app + landing GHCR

on:
  push:
    branches: [ "main" ]
    tags: [ "v*", "release-*" ]
  workflow_dispatch:    

env:
  REGISTRY: ghcr.io
  IMAGE_OWNER: jacke1012
  IMAGE_BASE: finance   # repo path after the owner

permissions: {}

jobs:
  docker:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    strategy:
      matrix:
        include:
          # --- image 1: app ---
          - name: app
            tag: app
            context: .
            file: ./Dockerfile          # adjust if your file is named differently
          # --- image 2: landing ---
          - name: landing
            tag: landing
            context: ./landing              # adjust to the folder you build from
            file: ./landing/Dockerfile      # adjust if needed

    steps:
      - uses: actions/checkout@v6

      #Only needed for multi aritecture builds
      #- uses: docker/setup-qemu-action@v3

      - uses: docker/setup-buildx-action@v4

      - name: Login to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push (${{ matrix.name }})
        uses: docker/build-push-action@v7
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.file }}
          platforms: linux/amd64
          push: ${{ github.event_name != 'pull_request' }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/${{ env.IMAGE_BASE }}:${{ matrix.tag }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/${{ env.IMAGE_BASE }}:${{ matrix.tag }}-${{ github.sha }}
          cache-from: type=gha,scope=${{ matrix.name }}
          cache-to: type=gha,scope=${{ matrix.name }},mode=max

  rollout:
    name: Trigger k3s rollout
    runs-on: ubuntu-latest
    needs: docker
    permissions: {}
    if: github.event_name != 'pull_request'

    steps:
      - name: Call rollout webhook
        env:
          K3S_WEBHOOK_URL: ${{ secrets.K3S_WEBHOOK_URL }}
          K3S_WEBHOOK_TOKEN: ${{ secrets.K3S_WEBHOOK_TOKEN }}
          K3S_NAMESPACE: ${{ secrets.K3S_NAMESPACE }}
          K3S_DEPLOYMENTS: ${{ secrets.K3S_DEPLOYMENTS }}
        run: |
          set -euo pipefail

          if [ -z "${K3S_WEBHOOK_URL}" ]; then
            echo "K3S_WEBHOOK_URL secret is not set."
            exit 1
          fi

          curl --fail --show-error --silent \
            --request POST "${K3S_WEBHOOK_URL}" \
            --header "Authorization: Bearer ${K3S_WEBHOOK_TOKEN}" \
            --header "Content-Type: application/json" \
            --data @- <<JSON
          {
            "repository": "${GITHUB_REPOSITORY}",
            "ref": "${GITHUB_REF_NAME}",
            "sha": "${GITHUB_SHA}",
            "namespace": "${K3S_NAMESPACE}",
            "deployments": "${K3S_DEPLOYMENTS}",
            "images": [
              "${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/${{ env.IMAGE_BASE }}:app-${GITHUB_SHA}",
              "${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/${{ env.IMAGE_BASE }}:landing-${GITHUB_SHA}"
            ]
          }
          JSON
